---
idea: Causal Sense: AI-native causal inference engine using LLMs to draft causal DAGs, expert validation via visual canvas, ATE estimation, and what-if simulation
goal: Pressure-test the idea against hackathon judging criteria and assess feature coherence before building
status: complete
updated: 2026-07-06T18:05
---

- (decision) Moving from 4 separate per-teammate agents to one shared-tools agent architecture to eliminate redundancy and enable tool reuse across features
- (direction) Proposed pipeline: Query+Data -> Intent Recognizer -> Solution Orchestrator (all tools) -> Final Answer Generator
- (crack) User is unsure whether causal inference is the core identity or one of four equal features — product identity not yet locked
- (direction) Market research role: search external events to ground causal hypotheses (e.g. gold price spike -> LLM suggests war/economic crisis as candidate causes)
- (lock) Market research (Atharva) runs UPSTREAM of DAG construction — searches for real-world causal candidates (events, context) to enrich the LLM's DAG draft. It is a tool inside the DAG-drafting step, not a standalone feature.
- (lock) Sahil's feature = post-causation 'what-if' simulator. Once ATE is estimated, user adjusts causal variables interactively to simulate interventions (do-calculus: P(Y|do(X))). This is the product's payoff moment — the reason all upstream work exists.
- (lock) 10-day hackathon timeline — full 7-step pipeline is feasible to build and demo
- (decision) Replacing LLM-based Intent Recognizer with explicit clickable UI mode selection — user picks analysis type directly rather than inferring from query text
- (lock) No LLM intent recognizer. User selects mode explicitly via UI buttons: [EDA] [Market Research] [Causal Analysis] [What-if Sim]. Orchestrator dispatches based on selection. AI adds value post-selection via proactive suggestions (e.g. 'spike detected — run causal analysis?'), not pre-selection routing.

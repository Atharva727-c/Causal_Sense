| Original Text | Revised Text | Changes |
|---|---|---|
| **DAG (Causal Graph)** — A directed acyclic graph of hypothesized causal relationships between variables in the user's dataset, drafted by the LLM and refined via Expert Validation. (§3 Glossary) | **DAG (Causal Graph)** — A directed acyclic graph of hypothesized causal relationships between variables in the user's dataset, drafted by the Drafting LLM and refined via Expert Validation. | Terminology consistency: the Glossary defines "Drafting LLM" and "Validator LLM" as the two specific named roles two entries later, but this entry still uses the generic "the LLM" — could read as a third, undefined LLM. |
| This was chosen over four separate per-teammate agents (one each for EDA, Market Research, causal core, What-if Sim), which was explicitly rejected — it would create redundant tools and prevent tool sharing across modes. (FR-13) | This was chosen over four separate per-teammate agents (one each for EDA, Market Research, causal core, What-if Sim), which were explicitly rejected — they would create redundant tools and prevent tool sharing across modes. | Subject-verb/pronoun agreement: "agents" is plural, so "which was... it would" should be "which were... they would." |
| it is also runnable standalone (a user wants event context for a topic/timeframe without running full causal analysis), but that standalone mode is a lighter, secondary path... (§4.2 Description) | it is also runnable standalone — e.g. when a user wants event context for a topic/timeframe without running full causal analysis — but that standalone mode is a lighter, secondary path... | Consider: the parenthetical currently reads as a condition attached to "runnable standalone" rather than an example of when a user would use it; an em-dash + "e.g." framing removes the ambiguity. |
